import { sharedArchitecture, usersAnswer } from '../src/index';

const gameCalc = () => {
    console.log('What is the result of the expression?');
    for (let i = 0; i < 3; i += 1) {
        sharedArchitecture()
    }
};